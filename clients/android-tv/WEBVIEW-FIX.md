# Исправление ошибки "SyntaxError" на Android 7.1.2

## 🔍 Проблема

На Android 7.1.2 появляется черный экран и в логах видно:

```
[WebView] Uncaught SyntaxError: Unexpected token (
[WebView] Uncaught SyntaxError: Unexpected token {
```

**Причина:** Старый Android System WebView не поддерживает современный JavaScript (ES6+):
- ❌ Template literals `` `text ${var}` ``
- ❌ Arrow functions `() => {}`
- ❌ Некоторые ES6 фичи

## ✅ Решение 1: Обновить Android System WebView (рекомендуется)

### Через Google Play:

1. На Android устройстве откройте **Google Play Store**
2. Найдите **"Android System WebView"**
3. Нажмите **"Обновить"**
4. Дождитесь установки
5. Перезапустите VCPlayer приложение

### Через ADB (если нет Google Play):

```bash
# Скачайте последнюю версию WebView APK с:
# https://www.apkmirror.com/apk/google-inc/android-system-webview/

# Установите через ADB:
adb install -r com.google.android.webview_xxx.apk

# Включите WebView:
adb shell pm enable com.google.android.webview

# Перезагрузите устройство:
adb reboot
```

### Проверьте версию WebView:

```bash
# Текущая версия
adb shell pm list packages -f | grep webview

# Версия должна быть 80+ для полной поддержки ES6
```

---

## ✅ Решение 2: Используйте другое устройство

**Рекомендуемые версии Android:**
- ✅ Android 8.0+ (API 26+) - полная поддержка
- ✅ Android 7.0-7.1 (API 24-25) - работает если WebView обновлен
- ⚠️ Android 6.0 (API 23) - требует обновление WebView
- ❌ Android 5.0-5.1 (API 21-22) - ограниченная поддержка

**Протестировано:**
- ✅ iconBIT DS2 (Android 7.0+)
- ✅ Lumien LS5550SD (Android 11.0)
- ✅ Generic Android TV Box (Android 9.0+)

---

## ❓ Как проверить версию Android на устройстве

### На самом устройстве:
```
Настройки → О системе → Версия Android
```

### Через ADB:
```bash
adb shell getprop ro.build.version.release
# Результат: 7.1.2 (ваш случай)
```

---

## 🛠️ Альтернатива: Использовать старую версию плеера

Если обновление WebView невозможно, используйте простой плеер без ES6:

```html
<!-- Вместо player-videojs.html используйте простой плеер -->
http://10.172.0.151/player-simple.html?device_id=0001
```

*Примечание: Простой плеер не включен в текущую версию, но может быть добавлен при необходимости.*

---

## 📝 Итог

**Для вашего устройства (Android 7.1.2):**

1. **Обновите Android System WebView** из Google Play
2. Или обновите прошивку Android до 8.0+
3. Или используйте другое устройство с Android 8.0+

**После обновления WebView:**
- ✅ JavaScript будет работать
- ✅ Регистрация на сервере пройдет
- ✅ Видео будет показываться
- ✅ Все функции заработают

---

## 🆘 Если Google Play недоступен

Обратитесь к производителю устройства за обновлением прошивки или используйте:
- F-Droid (альтернативный магазин)
- APKMirror (скачать WebView APK вручную)
- Кастомная прошивка (LineageOS и т.д.)

---

**Обновите WebView и все заработает!** ✅

