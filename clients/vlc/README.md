# VideoControl - VLC Client v2.0

Упрощенный и надежный клиент для воспроизведения видео через VLC плеер.

## 📦 Две версии клиента

### 1. **vlc_client.py** (основная версия)
- Использует библиотеку `python-vlc`
- Больше контроля над VLC
- Watchdog механизм для отслеживания состояния

### 2. **vlc_client_simple.py** (альтернативная)
- Запускает VLC как subprocess
- Без библиотеки `python-vlc`
- Проще, надежнее для проблемных систем
- **Рекомендуется если основная версия не работает**

## 🎯 Особенности

- ✅ **Минимализм** - только видео воспроизведение, без лишней логики
- ✅ **Надежность** - улучшенный watchdog механизм
- ✅ **Автовозврат** - автоматический возврат к заглушке после окончания видео
- ✅ **Кроссплатформенность** - Windows, Linux, macOS
- ✅ **Простота** - один файл, минимум зависимостей
- ✅ **URL encoding** - поддержка файлов с пробелами и кириллицей

## 📦 Поддерживаемые форматы

- **Видео:** mp4, webm, mkv, avi, mov, ogg
- **Заглушка:** видео файл `default.mp4` в папке устройства

## 🚀 Быстрая установка (Linux)

```bash
wget -qO- https://raw.githubusercontent.com/ya-k0v/VideoControl/main/scripts/quick-install-vlc.sh | bash
```

## 📋 Ручная установка

### 1. Установка зависимостей

**Ubuntu/Debian:**
```bash
sudo apt-get update
sudo apt-get install -y vlc python3-vlc python3-pip
pip3 install python-vlc python-socketio[client]>=5.10.0
```

**CentOS/RHEL:**
```bash
sudo yum install -y vlc python3-vlc python3-pip
pip3 install python-vlc python-socketio[client]>=5.10.0
```

**Windows:**
1. Установите VLC: https://www.videolan.org/vlc/
2. Установите Python 3.8+: https://www.python.org/downloads/
3. Откройте CMD:
```cmd
pip install python-vlc python-socketio[client]>=5.10.0
```

### 2. Загрузка клиента

```bash
mkdir -p ~/videocontrol-vlc
cd ~/videocontrol-vlc
curl -O https://raw.githubusercontent.com/ya-k0v/VideoControl/main/clients/vlc/vlc_client.py
chmod +x vlc_client.py
```

### 3. Запуск

**Основная версия (python-vlc):**
```bash
python3 vlc_client.py --server http://192.168.1.100 --device vlc-001
```

**Альтернативная версия (subprocess) - если основная не работает:**
```bash
python3 vlc_client_simple.py --server http://192.168.1.100 --device vlc-001
```

**Параметры:**
- `--server` - адрес сервера VideoControl (обязательно)
- `--device` - ID устройства (обязательно)
- `--no-fullscreen` - запуск в оконном режиме (опционально)

**Какую версию выбрать?**
- ✅ Используйте `vlc_client.py` по умолчанию
- ✅ Используйте `vlc_client_simple.py` если:
  - VLC окно закрывается при ошибках
  - Проблемы с библиотекой python-vlc
  - Нужна максимальная стабильность

## 🔧 Systemd Service (Linux)

Создайте файл `/etc/systemd/system/videocontrol-vlc.service`:

```ini
[Unit]
Description=VideoControl VLC Client
After=network.target

[Service]
Type=simple
User=pi
WorkingDirectory=/home/pi/videocontrol-vlc
Environment=DISPLAY=:0
Environment=XAUTHORITY=/home/pi/.Xauthority
ExecStart=/usr/bin/python3 /home/pi/videocontrol-vlc/vlc_client.py --server http://192.168.1.100 --device vlc-001
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

Запуск:
```bash
sudo systemctl daemon-reload
sudo systemctl enable videocontrol-vlc
sudo systemctl start videocontrol-vlc
```

Проверка:
```bash
sudo systemctl status videocontrol-vlc
sudo journalctl -u videocontrol-vlc -f
```

## 📊 Логирование

Клиент выводит информативные логи:

```
[VLC] 🚀 Запуск VLC клиента v2.0
[VLC] Сервер: http://192.168.1.100
[VLC] Устройство: vlc-001
[VLC] ✅ Подключено к серверу
[VLC] 📡 Зарегистрирован как плеер
[VLC] 🔁 Загрузка заглушки: http://192.168.1.100/content/vlc-001/default.mp4
[VLC] ✅ Заглушка запущена
[VLC] ▶️ PLAY: type=video, file=video.mp4
[VLC] 🎬 Загрузка видео: http://192.168.1.100/content/vlc-001/video.mp4
[VLC] ✅ Видео запущено
[VLC] 🏁 Медиа закончилось
[VLC] 🔄 Возврат к заглушке
```

## ⚠️ Важно

### PDF/PPTX/Изображения
VLC клиент **НЕ поддерживает** PDF, PPTX и статические изображения.  
Для этого используйте **browser-based плеер** (player-videojs.html).

### Отличия от browser плеера

| Возможность | VLC Client | Browser Player |
|-------------|------------|----------------|
| Видео | ✅ | ✅ |
| Изображения | ❌ | ✅ |
| PDF | ❌ | ✅ |
| PPTX | ❌ | ✅ |
| Нативное воспроизведение | ✅ | ❌ |
| Меньше ресурсов | ✅ | ❌ |
| Fullscreen без браузера | ✅ | ❌ |

## 🐛 Troubleshooting

### VLC не запускается в fullscreen
```bash
# Проверьте права доступа к X server:
xhost +local:

# Убедитесь что DISPLAY и XAUTHORITY правильные:
echo $DISPLAY  # должно быть :0
echo $XAUTHORITY
```

### Ошибка подключения к серверу
```bash
# Проверьте доступность сервера:
curl http://192.168.1.100/api/devices

# Проверьте порт (если используете прямое подключение):
curl http://192.168.1.100:3000/api/devices
```

### Видео зависает на последнем кадре
- VLC клиент v2.0 имеет улучшенный watchdog механизм
- Проверка зависания каждые 0.5 сек
- Автоматический возврат к заглушке через 3 сек зависания

### Заглушка не найдена
```bash
# Убедитесь что заглушка существует:
ls -lh /vid/videocontrol/public/content/vlc-001/default.mp4

# Установите заглушку через admin панель
```

## 📝 Changelog

### v2.0 (2025-11-05)
- ✅ Полная переработка под текущий проект
- ✅ Упрощенная архитектура (только видео)
- ✅ Улучшенный watchdog механизм
- ✅ Обработка события `placeholder/refresh`
- ✅ Heartbeat механизм
- ✅ Лучшее логирование
- ❌ Удалена поддержка PDF/PPTX (используйте browser плеер)

### v1.0 (2024)
- Первая версия с поддержкой PDF/PPTX

## 🔗 Ссылки

- [Главная документация](../../README.md)
- [Android TV Client](../android-tv/README.md)
- [Server Setup](../../INSTALL.md)

---

**Автор:** ya-k0v  
**Лицензия:** MIT  
**Версия:** 2.0
