# VideoControl - Быстрый старт

> Установка и запуск за 5 минут

---

## 🚀 Установка

### На чистый Ubuntu/Debian сервер:

```bash
curl -fsSL https://raw.githubusercontent.com/ya-k0v/VideoControl/main/scripts/quick-install.sh | sudo bash
```

**Готово!** Сервер запущен на `http://YOUR_IP/admin.html`

---

## 📱 Подключение устройств

### 1. Android TV Box / Телефон:

**Способ A: Готовый APK**
```bash
# Скачайте APK из установленного сервера
wget http://YOUR_SERVER_IP/VCMplayer-v2.5.0.apk

# Установите на устройство через adb
adb install VCMplayer-v2.5.0.apk

# Или скопируйте на флешку и установите вручную
```

**Способ B: Сборка из исходников**
```bash
cd clients/android-mediaplayer
./gradlew assembleDebug
adb install app/build/outputs/apk/debug/app-debug.apk
```

**Настройка:**
1. Запустите приложение
2. Введите URL сервера: `http://YOUR_SERVER_IP`
3. Введите Device ID: `ATV001` (любое имя)
4. Сохранить

### 2. Linux Desktop / Raspberry Pi:

```bash
# Автоматическая установка MPV клиента
curl -fsSL https://raw.githubusercontent.com/ya-k0v/VideoControl/main/clients/mpv/quick-install.sh | \
  bash -s -- --server http://YOUR_SERVER_IP --device mpv-001
```

### 3. Браузер (любое устройство):

Откройте: `http://YOUR_SERVER_IP/player-videojs.html?device_id=browser-001`

---

## 🎬 Добавление контента

### Через Admin панель:

1. Откройте `http://YOUR_SERVER_IP/admin.html`
2. Нажмите "+ Add Device"
3. Введите ID и название устройства
4. Drag & Drop файлы в окно загрузки
5. Нажмите "Upload"

**Поддерживаются:** MP4, WebM, AVI, MKV, MOV, PNG, JPG, PDF, PPTX, ZIP (папки)

### Через панель Speaker:

1. Откройте `http://YOUR_SERVER_IP/speaker.html`  
2. Выберите устройство
3. Кликните на файл для воспроизведения
4. Используйте кнопки управления (Play, Pause, Stop)

---

## 🛠️ Управление сервером

### Основные команды:

```bash
# Статус
sudo systemctl status videocontrol

# Перезапуск
sudo systemctl restart videocontrol

# Остановка
sudo systemctl stop videocontrol

# Логи (live)
sudo journalctl -u videocontrol -f

# Логи (последние 100 строк)
sudo journalctl -u videocontrol -n 100
```

### Проверка работы:

```bash
# Проверить что сервер отвечает
curl http://localhost/api/system/info

# Проверить список устройств
curl http://localhost/api/devices

# Проверить Nginx
sudo systemctl status nginx
```

---

## 📁 Структура файлов

```
/vid/videocontrol/
├── config/
│   ├── main.db                 ← База данных SQLite
│   └── video-optimization.json ← Настройки оптимизации
│
├── public/content/
│   ├── ATV001/                 ← Файлы для устройства ATV001
│   ├── mpv-001/                ← Файлы для MPV клиента
│   └── browser-001/            ← Файлы для браузера
│
└── .converted/                 ← Кэш PDF/PPTX (автоматически)
```

---

## ⚙️ Настройка

### Видео оптимизация:

Отредактируйте `config/video-optimization.json`:

```json
{
  "enabled": true,           // Включить автооптимизацию
  "autoOptimize": true,      // Автоматически после загрузки
  "deleteOriginal": true,    // Удалять оригинал после оптимизации
  "defaultProfile": "1080p"  // Целевое разрешение (720p или 1080p)
}
```

После изменений: `sudo systemctl restart videocontrol`

### Заглушка (placeholder):

В Admin панели → выберите устройство → загрузите файл `default.mp4` или `default.jpg`

Заглушка автоматически зациклится и будет показываться когда нет активного контента.

---

## 🔧 Решение проблем

### Сервер не запускается:

```bash
# Проверить логи
journalctl -u videocontrol -n 50

# Проверить что порт 3000 свободен
ss -tlnp | grep 3000

# Запустить вручную для отладки
cd /vid/videocontrol
node server.js
```

### Android приложение не подключается:

1. Проверьте что сервер доступен: `ping YOUR_SERVER_IP`
2. Проверьте URL в настройках: должен быть `http://IP` (не https)
3. Проверьте логи приложения: `adb logcat | grep VCMediaPlayer`

### Медленная загрузка файлов:

```bash
# Запустите оптимизацию TCP
cd /vid/videocontrol
sudo ./scripts/optimize-network.sh
```

После оптимизации: 1.5 GB файл загружается ~15-30 секунд вместо 2-5 минут.

---

## 📚 Дополнительная информация

- 📘 [Полная документация](README.md)
- 💾 [Требования к железу](docs/HARDWARE_REQUIREMENTS.md)
- 💿 [Настройка отдельного диска](docs/DATA_DISK_SETUP.md)
- 📱 [Android приложение](docs/ANDROID.md)
- 🖥️ [Linux MPV клиент](clients/mpv/README.md)

---

## 🆘 Поддержка

- 🐛 [Issues на GitHub](https://github.com/ya-k0v/VideoControl/issues)
- 📖 [Wiki](https://github.com/ya-k0v/VideoControl/wiki)
- ⭐ [Поставить звезду](https://github.com/ya-k0v/VideoControl)

