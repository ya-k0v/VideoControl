# VCPlayer - Changelog

## Version 1.0.5 - 2025-11-06

### 🎬 Постоянный черный слой под WebView

**Проблема:**
При смене контента (особенно видео) Android показывал стандартную кнопку "Play" на белом/сером фоне, даже при черном фоне WebView. Это происходило потому что WebView не имел постоянного черного контента под собой.

**Решение:**
Создан двухслойный layout с постоянным черным View под WebView:
1. **Нижний слой** - постоянный черный View (всегда видимый)
2. **Верхний слой** - WebView (работает поверх черного)

**Результат:**
- ✅ Стандартная кнопка Play Android больше не видна
- ✅ Черный фон всегда присутствует как контент
- ✅ WebView работает поверх постоянного черного слоя
- ✅ Нет белого/серого фона при любых переходах

**Технические изменения:**

activity_main.xml:
```xml
<FrameLayout>
    <!-- Постоянный черный слой -->
    <View
        android:id="@+id/blackBackground"
        android:background="#000000" />
    
    <!-- WebView поверх черного -->
    <FrameLayout
        android:id="@+id/webViewContainer" />
</FrameLayout>
```

MainActivity.kt:
```kotlin
// Используем layout вместо программного создания
setContentView(R.layout.activity_main)

// Получаем контейнер
val webViewContainer = findViewById<FrameLayout>(R.id.webViewContainer)

// Добавляем WebView в контейнер поверх черного слоя
webView?.let { webViewContainer.addView(it) }
```

**Файлы:**
- `/vid/videocontrol/clients/android-tv/VideoControlTV/app/src/main/res/layout/activity_main.xml`
- `/vid/videocontrol/clients/android-tv/VideoControlTV/app/src/main/java/com/videocontrol/tv/MainActivity.kt`

---

## Version 1.0.4 - 2025-11-06

### 🎨 Оптимизация черного фона

**Проблема:**
При смене контента в WebView мог появляться белый фон по умолчанию, что создавало вспышки между переходами.

**Решение:**
Добавлена принудительная установка черного фона для WebView:
1. **setBackgroundColor(BLACK)** при создании WebView
2. **onPageStarted** - черный фон во время загрузки
3. **onPageFinished** - черный фон после загрузки
4. Двойная буферизация на веб-стороне (img1/img2) для плавных переходов

**Результат:**
- ✅ Черный экран всегда во время смены контента
- ✅ Нет белых вспышек WebView
- ✅ Плавные переходы между видео/презентациями/изображениями
- ✅ Кросс-фейд для смены типов контента
- ✅ Мгновенное листание слайдов презентаций

**Технические изменения:**

MainActivity.kt:
```kotlin
webView = WebView(this).apply {
    // Черный фон WebView
    setBackgroundColor(android.graphics.Color.BLACK)
    
    webViewClient = object : WebViewClient() {
        override fun onPageStarted(...) {
            // Черный фон во время загрузки
            view?.setBackgroundColor(android.graphics.Color.BLACK)
        }
        
        override fun onPageFinished(...) {
            // Черный фон после загрузки
            view?.setBackgroundColor(android.graphics.Color.BLACK)
        }
    }
}
```

**Файлы:**
- `/vid/videocontrol/clients/android-tv/VideoControlTV/app/src/main/java/com/videocontrol/tv/MainActivity.kt`
- `/vid/videocontrol/public/player-videojs.html` (двойная буферизация)
- `/vid/videocontrol/public/js/player-videojs.js` (img1/img2)

---

## Version 1.0.3 - 2025-11-05

### 🎬 Плавные переходы (Crossfade)

**Проблема:**
Большая кнопка PLAY от Video.js появлялась в момент предзагрузки видео при переключении контента.

**Решение:**
Внедрен плавный crossfade эффект для всех типов контента:
1. **Fade out** текущего контента (0.5s)
2. **Загрузка** нового контента в скрытом слое
3. **Fade in** нового контента (0.5s) только когда готов

**Результат:**
- ✅ Кнопка PLAY больше не видна (скрыта во время fade)
- ✅ Всегда черный фон во время переходов
- ✅ Плавные переключения видео, изображений, презентаций

**Технические изменения:**

CSS (`player-videojs.html`):
```css
.layer {
  transition: opacity 0.5s ease-in-out;
}

.layer.visible {
  opacity: 1;  /* Видимый контент */
}

.layer.preloading {
  opacity: 0;  /* Загружается в фоне */
}
```

JavaScript (`player-videojs.js`):
- Добавлена функция `show(el)` - плавный показ с fade in
- Добавлена функция `preload(el)` - подготовка к показу (скрыто)
- Все загрузки контента теперь через `preload()` → `loadedmetadata/onload` → `show()`

**Файлы:**
- `/vid/videocontrol/public/player-videojs.html`
- `/vid/videocontrol/public/js/player-videojs.js`

**Настройка скорости:**
Изменить в `player-videojs.html` строка 57:
```css
transition: opacity 0.5s ease-in-out;
/* 0.3s - быстрее, 0.7s - медленнее */
```

---

## Version 1.0.2 - 2025-11-05

### Исправления 🐛

#### 1. Убрана большая кнопка PLAY при переключении видео

**Проблема:**
При переключении видео появлялась большая стандартная кнопка PLAY от Video.js или Android WebView.

**Решение:**
- Добавлена функция `hideVideoJsControls()` в `player-videojs.js`
- Программное скрытие контролов через Video.js API:
  - `bigPlayButton` - большая кнопка в центре
  - `controlBar` - панель управления
  - `loadingSpinner` - индикатор загрузки
- Вызывается при инициализации, перед каждой сменой src, после loadedmetadata
- Упрощена JavaScript инъекция в Android MainActivity.kt
- Добавлен дополнительный CSS для принудительного скрытия контролов

**Файлы:**
- `/vid/videocontrol/public/js/player-videojs.js`
- `/vid/videocontrol/clients/android-tv/VideoControlTV/app/src/main/java/com/videocontrol/tv/MainActivity.kt`

---

#### 2. Исправлен черный экран при первом пролистывании презентации

**Проблема:**
При первом переключении слайдов PPTX/PDF появлялся черный экран, хотя при повторном пролистывании уже показанных слайдов все работало.

**Решение:**
- Изменена логика `showConvertedPage()` в `player-videojs.js`
- Добавлена предзагрузка через временный `Image` объект
- Слайд показывается ТОЛЬКО после события `onload`
- Предотвращает показ пустого `img` элемента до полной загрузки изображения
- Сохранена фоновая предзагрузка всех слайдов в кэш для мгновенного переключения

**Файлы:**
- `/vid/videocontrol/public/js/player-videojs.js`

---

### Технические детали

#### hideVideoJsControls()

```javascript
function hideVideoJsControls() {
  if (!vjsPlayer) return;
  
  try {
    // Скрываем big play button
    const bigPlayButton = vjsPlayer.getChild('bigPlayButton');
    if (bigPlayButton) {
      bigPlayButton.hide();
      bigPlayButton.el().style.display = 'none';
    }
    
    // Скрываем control bar
    const controlBar = vjsPlayer.getChild('controlBar');
    if (controlBar) {
      controlBar.hide();
      controlBar.el().style.display = 'none';
    }
    
    // Скрываем loading spinner
    const loadingSpinner = vjsPlayer.getChild('loadingSpinner');
    if (loadingSpinner) {
      loadingSpinner.hide();
      loadingSpinner.el().style.display = 'none';
    }
    
    console.log('[Player] 🚫 Все контролы Video.js скрыты');
  } catch (e) {
    console.warn('[Player] ⚠️ Ошибка скрытия контролов:', e);
  }
}
```

Вызывается в:
- `vjsPlayer.ready()` - при инициализации
- Перед каждым `vjsPlayer.src()` - перед сменой источника
- После `loadedmetadata` - когда метаданные загружены

#### showConvertedPage() с предзагрузкой

```javascript
function showConvertedPage(file, type, num) {
  // Проверка кэша...
  
  // Fallback: загрузка через API с предзагрузкой
  const tempImg = new Image();
  tempImg.onload = () => {
    console.log(`[Player] ✅ Слайд ${num} загружен, показываем`);
    img.src = imageUrl;
    show(img);
  };
  tempImg.onerror = () => {
    console.error(`[Player] ❌ Ошибка загрузки слайда ${num}`);
    img.src = imageUrl;
    show(img);
  };
  tempImg.src = imageUrl;
}
```

---

### Установка обновления

```bash
# 1. Обновите сервер
cd /vid/videocontrol
git pull
sudo systemctl restart videocontrol

# 2. Пересоберите и установите APK
cd clients/android-tv
bash build-apk.sh
adb install -r VCPlayer.apk
```

---

### Проверка

После установки проверьте в логах:

**Browser Console (F12):**
```
[Player] 🚫 Все контролы Video.js скрыты
[Player] ✅ Слайд N загружен, показываем
```

**Android Logcat:**
```
[Android] ✅ Sound enabled in localStorage
[Android] ✅ Unmute button hidden
[Android] ✅ Media controls CSS injected
[Android] ✅ Initialization complete
```

---

## Version 1.0.1 - 2025-11-04

### Изменения
- Первый релиз с подписанным APK
- Название приложения изменено на "VCPlayer"
- Добавлена автоматическая сборка через `build-apk.sh`

---

**Разработчик:** ya-k0v  
**Репозиторий:** https://github.com/ya-k0v/VideoControl

