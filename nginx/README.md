# Nginx Configuration

Nginx для production - ускорение раздачи контента в 5-10 раз.

## Установка

```bash
cd nginx
sudo bash install-nginx.sh
```

## Что делает

- Слушает порт 80 (HTTP)
- Раздает `/content/*` напрямую (быстро!)
- Проксирует API и WebSocket на Node.js:3000
- Кэширует статику
- Поддерживает Range requests для видео

## Конфигурация

Файл: `/etc/nginx/sites-available/videocontrol.conf`

**Настройки:**
- Node.js порт: 3000
- Nginx порт: 80
- Client max body size: 1GB
- Timeouts: оптимизированы для больших файлов

## Управление

```bash
# Перезапуск
sudo systemctl restart nginx

# Статус
sudo systemctl status nginx

# Проверка конфигурации
sudo nginx -t
```

## Преимущества

- ⚡ Скорость: 500-1000 MB/s vs 50-100 MB/s (Node.js)
- 📊 Соединения: 100+ vs 10-20 (Node.js)
- 🎯 HTTP оптимизация для видео
- 🔄 Range requests support

## Требования

- Nginx 1.18+
- Node.js сервер на порту 3000
