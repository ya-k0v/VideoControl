# VideoControl VLC Client

Универсальный клиент для воспроизведения видео контента через VLC плеер.

## 🎯 Возможности

- ✅ Воспроизведение видео через VLC
- ✅ Автоматическая заглушка (loop default.mp4)
- ✅ Real-time управление через WebSocket
- ✅ Автоматический реконнект при потере связи
- ✅ Fullscreen режим
- ✅ Кроссплатформенность (Windows, Linux, macOS)
- ✅ Systemd integration для Linux

## 📦 Установка

### 1. Установить VLC

**Ubuntu/Debian:**
```bash
sudo apt install vlc python3-vlc
```

**macOS:**
```bash
brew install --cask vlc
```

**Windows:**
Скачать с https://www.videolan.org/vlc/

### 2. Установить Python зависимости

```bash
cd /vid/videocontrol/clients/vlc
pip3 install -r requirements.txt
```

## 🚀 Запуск

### Ручной запуск (через Nginx):

```bash
python3 vlc_client.py --server http://192.168.1.100 --device vlc-001
```

### С параметрами:

```bash
# С отладкой (через Nginx)
python3 vlc_client.py --server http://localhost --device test-vlc --debug

# Без fullscreen (для тестирования)
python3 vlc_client.py --server http://localhost --device vlc-test --no-fullscreen

# Напрямую к Node.js (только для разработки БЕЗ Nginx)
python3 vlc_client.py --server http://localhost:3000 --device test --debug
```

### Через переменные окружения:

```bash
export VIDEOCONTROL_SERVER="http://192.168.1.100"
export VIDEOCONTROL_DEVICE_ID="vlc-hall-001"
python3 vlc_client.py
```

## ⚙️ Systemd Service (автозапуск)

### 1. Отредактировать service файл:

```bash
nano videocontrol-vlc@.service
```

Замените:
- `YOUR_USERNAME` → ваше имя пользователя
- `VIDEOCONTROL_SERVER` → URL вашего сервера
- Путь к Python если нужно

### 2. Установить service:

```bash
sudo cp videocontrol-vlc@.service /etc/systemd/system/
sudo systemctl daemon-reload
```

### 3. Запустить для конкретного устройства:

```bash
# Для устройства "tv-hall-001"
sudo systemctl enable videocontrol-vlc@tv-hall-001
sudo systemctl start videocontrol-vlc@tv-hall-001

# Проверить статус
sudo systemctl status videocontrol-vlc@tv-hall-001

# Логи
sudo journalctl -u videocontrol-vlc@tv-hall-001 -f
```

## 📋 Параметры командной строки

| Параметр | Описание | Значение по умолчанию |
|----------|----------|-----------------------|
| `--server`, `-s` | URL сервера VideoControl | http://localhost (порт 80 через Nginx) |
| `--device`, `-d` | ID устройства | vlc-001 |
| `--no-fullscreen` | Не запускать fullscreen | false |
| `--debug` | Отладочные сообщения | false |

## 🔄 Как это работает

1. **Запуск:** Клиент запускается и сразу начинает крутить заглушку `default.mp4` в loop
2. **Подключение:** Подключается к серверу через WebSocket (Socket.IO)
3. **Регистрация:** Регистрируется как устройство с типом 'vlc'
4. **Ожидание:** Ждет команды от сервера (play, pause, stop, restart)
5. **Контент:** При команде `play` воспроизводит указанный файл
6. **Завершение:** После окончания контента возвращается к заглушке
7. **Автономность:** При потере связи продолжает крутить заглушку

## 🎬 Пример работы

```
[2025-11-02 15:30:00] 🚀 Запуск VLC клиента
[2025-11-02 15:30:00]    Сервер: http://192.168.1.100:3000
[2025-11-02 15:30:00]    Устройство: vlc-001
[2025-11-02 15:30:00]    Заглушка: http://192.168.1.100:3000/content/vlc-001/default.mp4
[2025-11-02 15:30:01] 🔌 Подключение к серверу...
[2025-11-02 15:30:01] ✅ Подключено к серверу http://192.168.1.100:3000
[2025-11-02 15:30:01] ✅ Клиент запущен. Для выхода нажмите Ctrl+C
[2025-11-02 15:31:15] ▶️  Команда PLAY: {'file': 'promo.mp4', 'type': 'video'}
[2025-11-02 15:31:15] 🎬 Загрузка контента: http://192.168.1.100:3000/content/vlc-001/promo.mp4
[2025-11-02 15:33:45] 📺 Видео закончилось
[2025-11-02 15:33:45] 🔄 Возврат к заглушке...
```

## 🐛 Troubleshooting

### VLC не запускается

```bash
# Проверить что VLC установлен
vlc --version

# Проверить Python модуль
python3 -c "import vlc; print(vlc.__version__)"
```

### Не подключается к серверу

```bash
# Проверить доступность сервера через Nginx (порт 80)
curl http://192.168.1.100/

# Запустить с отладкой
python3 vlc_client.py --server http://192.168.1.100 --device test --debug

# Или напрямую к Node.js (если Nginx не установлен)
python3 vlc_client.py --server http://192.168.1.100:3000 --device test --debug
```

### Заглушка не найдена

Убедитесь что файл `default.mp4` существует в папке контента устройства:
```bash
ls -la /vid/videocontrol/public/content/vlc-001/default.mp4
```

### Permission denied для DISPLAY

Если запускаете через systemd:
```bash
# Разрешить доступ к X11
xhost +local:

# Или запускать от имени пользователя с активным X-сервером
```

## 📊 Системные требования

- **Python:** 3.7+
- **VLC:** 3.0+
- **RAM:** 100-200 MB
- **CPU:** Минимальные (VLC эффективен)
- **ОС:** Linux, macOS, Windows

## 🔧 Настройка

### Изменить URL заглушки

Заглушка определяется автоматически:
```
{server_url}/content/{device_id}/default.mp4
```

### Кастомные VLC параметры

Отредактируйте `vlc_client.py`, строка ~42:
```python
vlc_args = [
    '--no-video-title-show',
    '--quiet',
    '--no-osd'
]
```

## 🚀 Production рекомендации

1. **Используйте systemd service** для автозапуска
2. **Настройте логирование** через journald
3. **Мониторьте статус** через `systemctl status`
4. **Используйте отдельного пользователя** для безопасности
5. **Настройте автообновление** заглушки

## 📝 Лицензия

Часть проекта VideoControl

