# VideoControl Android TV Client

Нативное Android приложение для отображения видео контента на Android устройствах.

## 🎯 Поддерживаемые устройства

- **iconBIT DS2** (Android)
- **Lumien LS5550SD** (Android 11.0)
- **Любые Android TV боксы** (Android 5.0+)
- **Планшеты и телефоны Android**

## ✨ Возможности

- ✅ **Чистый fullscreen** - без кнопок и overlay
- ✅ **Автозапуск** при включении устройства
- ✅ **Автовключение звука** - без кнопки unmute
- ✅ **Watchdog сервис** - автоперезапуск при сбоях
- ✅ **Kiosk режим** - блокировка кнопки назад
- ✅ **Hardware acceleration** для плавного видео
- ✅ **WebView оптимизация** под медиа

## 🚀 Быстрая установка

### 1. Собрать APK

```bash
cd clients/android-tv/VideoControlTV

# Измените URL сервера в build.gradle (строка 18)
# buildConfigField "String", "DEFAULT_SERVER_URL", '"http://192.168.1.101"'

# Windows
build.ps1

# Linux/macOS
./gradlew assembleRelease

# APK: app/build/outputs/apk/release/app-release.apk
```

### 2. Установить на устройства

#### Через ADB (по WiFi/USB)

```bash
# Включите Developer Mode на Android устройстве:
# Настройки → О системе → Версия сборки (7 раз нажать)
# Настройки → Для разработчиков → Отладка по USB

# Через USB
adb install app-release.apk

# Через WiFi (узнайте IP устройства)
adb connect 192.168.1.101:5555
adb install app-release.apk
```

#### Массовая установка (скрипт)

```bash
#!/bin/bash
# mass-install.sh

SERVER="http://192.168.1.101"
APK="app-release.apk"

# iconBIT DS2 - 2 шт
echo "Установка на iconBIT DS2..."
adb connect 192.168.1.101:5555 && adb -s 192.168.1.101:5555 install $APK
adb connect 192.168.1.102:5555 && adb -s 192.168.1.102:5555 install $APK

# Lumien LS5550SD - 8 шт
echo "Установка на Lumien LS5550SD..."
for i in {1..8}; do
    IP="192.168.1.$(( 110 + i ))"
    echo "Устройство $i: $IP"
    adb connect $IP:5555
    adb -s $IP:5555 install $APK
    sleep 2
done

echo "✓ Установка завершена на все устройства!"
```

### 3. Настройка при первом запуске

При первом запуске приложение откроет экран настройки:

1. **Server URL**: `http://192.168.1.101` (ваш сервер)
2. **Device ID**: уникальный ID (например: `lumien-01`, `iconbit-01`)
3. Нажмите **Save**

Приложение автоматически запустится и зарегистрируется на сервере!

---

## 🔧 Дополнительные настройки

### Автозапуск при включении

Приложение автоматически запускается при загрузке Android (через `BootReceiver`).

Если не работает:
1. Настройки → Приложения → VideoControl
2. Разрешения → Автозапуск: **Включить**
3. Оптимизация батареи: **Не оптимизировать**

### Настройка через ADB (без GUI)

```bash
# Установить конфигурацию через ADB
adb shell "echo 'http://192.168.1.101' > /data/data/com.videocontrol.tv/shared_prefs/videocontrol.xml"

# Или через am broadcast
adb shell am broadcast -a com.videocontrol.tv.CONFIG \
    --es server_url "http://192.168.1.101" \
    --es device_id "lumien-01"
```

### Логи и диагностика

```bash
# Просмотр логов
adb logcat | grep VideoControl

# Очистить данные (сбросить настройки)
adb shell pm clear com.videocontrol.tv

# Перезапустить приложение
adb shell am force-stop com.videocontrol.tv
adb shell am start -n com.videocontrol.tv/.MainActivity
```

---

## 📋 Чек-лист установки на 10 устройств

- [ ] 1. Собрать APK с правильным SERVER_URL
- [ ] 2. Подготовить список IP адресов устройств
- [ ] 3. Включить Developer Mode на всех устройствах
- [ ] 4. Включить ADB по сети на всех устройствах
- [ ] 5. Запустить mass-install.sh
- [ ] 6. Настроить Device ID на каждом устройстве:
  - [ ] iconbit-01, iconbit-02
  - [ ] lumien-01, lumien-02, lumien-03, lumien-04
  - [ ] lumien-05, lumien-06, lumien-07, lumien-08
- [ ] 7. Проверить что все устройства появились в админке
- [ ] 8. Загрузить default.mp4 заглушку для каждого
- [ ] 9. Проверить автозапуск (перезагрузить устройство)
- [ ] 10. Готово! 🎉

---

## 🐛 Troubleshooting

### Проблема: Большая кнопка Play

**Решение:** Обновите приложение до версии с фиксом overlay (текущая версия).

### Проблема: Нет звука

**Решение:** Параметр `&sound=1` уже добавлен в URL, звук включится автоматически.

### Проблема: Видео зависает после окончания

**Решение:** Заглушка автоматически включится (реализовано в player.html).

### Проблема: Приложение не запускается автоматически

**Решение:** 
1. Отключите оптимизацию батареи
2. Дайте разрешение на автозапуск
3. Проверьте BootReceiver в логах

---

## 📦 Сборка разных версий

### Debug версия (для тестирования)

```bash
./gradlew assembleDebug
# APK: app/build/outputs/apk/debug/app-debug.apk
```

### Release версия (для production)

```bash
./gradlew assembleRelease
# APK: app/build/outputs/apk/release/app-release.apk
```

### Подписанная версия (для Play Store)

```bash
# Создайте keystore
keytool -genkey -v -keystore videocontrol.keystore -alias videocontrol -keyalg RSA -keysize 2048 -validity 10000

# Подпишите APK
jarsigner -verbose -sigalg SHA1withRSA -digestalg SHA1 -keystore videocontrol.keystore app-release.apk videocontrol
```

---

## 🔗 Полезные ссылки

- **Главный README**: [../../README.md](../../README.md)
- **Сервер установка**: [../../scripts/install-server.sh](../../scripts/install-server.sh)
- **Android Developer**: https://developer.android.com/studio/command-line/adb

---

**Версия:** 1.0.1  
**Дата:** 2025-11-05

