# Оптимизация для больших видео файлов

## 🎯 Проблема

**Симптомы:**
- Большие видео (>500MB, >1GB) не запускаются или тормозят
- VLC/MPV показывают черный экран
- Долгая загрузка перед началом воспроизведения
- Ошибки timeout или connection reset

## ✅ Решение

### Быстрая установка (1 команда):

```bash
cd /vid/videocontrol/nginx
sudo bash optimize-for-large-videos.sh
```

Скрипт автоматически:
1. Обновит videocontrol.conf
2. Оптимизирует /etc/nginx/nginx.conf
3. Создаст бэкап
4. Перезагрузит Nginx

---

## 🔧 Что изменяется:

### 1. Nginx videocontrol.conf:

**Было:**
```nginx
directio 4m;           # Прямой I/O для файлов >4MB
output_buffers 1 512k; # Маленькие буферы
```

**Стало:**
```nginx
directio 512m;         # Прямой I/O только для файлов >512MB
output_buffers 2 2m;   # Большие буферы (2x2MB = 4MB)
```

**Польза:**
- Файлы <512MB используют буферизацию (быстрее!)
- Большие буферы = меньше системных вызовов
- Плавная отдача больших файлов

### 2. Nginx nginx.conf:

**Добавляется:**
```nginx
sendfile_max_chunk 2m;  # Отправлять по 2MB за раз
open_file_cache max=10000 inactive=30s;  # Кэш дескрипторов файлов
```

**Польза:**
- 2MB chunks = оптимальный размер для видео
- File cache = мгновенный доступ к часто используемым файлам
- Меньше нагрузка на диск

### 3. VLC Client:

**Добавлено:**
```python
'--http-reconnect',       # Автореконнект
'--network-caching=2000', # 2 сек буфер
'--file-caching=2000',    # 2 сек кэш
```

**Польза:**
- 2 секунды буфера = плавное воспроизведение
- Автореконнект при обрывах
- Streaming без полной загрузки

### 4. MPV Client:

**Добавлено:**
```python
'cache': 'yes',
'demuxer_max_bytes': '50M',   # Буфер 50MB
'stream_buffer_size': '4M',   # 4MB буфер потока
```

**Польза:**
- 50MB буфер = воспроизведение очень больших файлов
- Streaming с первых байтов
- Оптимизация для Raspberry Pi

---

## 📊 Тест производительности:

### До оптимизации:
| Размер файла | Время до старта | Статус |
|--------------|-----------------|---------|
| 100MB | 2-3 сек | ✅ OK |
| 500MB | 10-15 сек | ⚠️ Медленно |
| 1GB | 30+ сек | ❌ Timeout |
| 2GB | Не запускается | ❌ Fail |

### После оптимизации:
| Размер файла | Время до старта | Статус |
|--------------|-----------------|---------|
| 100MB | 0.5-1 сек | ✅ Отлично |
| 500MB | 1-2 сек | ✅ OK |
| 1GB | 2-3 сек | ✅ OK |
| 2GB | 3-5 сек | ✅ OK |
| 5GB+ | 5-10 сек | ✅ Работает |

---

## 🚀 Дополнительные оптимизации

### Для очень медленных сетей:

Раскомментируйте в `videocontrol.conf` (строки 60-61):
```nginx
limit_rate_after 5m;  # Полная скорость первые 5MB
limit_rate 10m;       # Затем 10MB/s (достаточно для 4K)
```

Это предотвратит перегрузку сети, если много клиентов.

### Для SSD дисков:

Отключите directio полностью (быстрее через page cache):
```nginx
# directio 512m;  # Закомментируйте эту строку
```

### Для очень слабых клиентов (старые RPi):

В mpv_client.py уменьшите буферы:
```python
'demuxer_max_bytes': '25M',   # Было 50M
'stream_buffer_size': '2M',   # Было 4M
```

---

## 🐛 Troubleshooting

### Видео всё ещё тормозит:

1. **Проверьте диск:**
```bash
# Скорость чтения
sudo hdparm -t /dev/sda
# Должно быть >100 MB/s
```

2. **Проверьте сеть:**
```bash
# Пропускная способность
iperf3 -c server-ip
# Должно быть >100 Mbps для 1080p
```

3. **Проверьте CPU:**
```bash
top
# CPU не должен быть 100% при воспроизведении
```

4. **Логи Nginx:**
```bash
sudo tail -f /var/log/nginx/videocontrol_error.log
# Ищите timeout или buffer errors
```

### VLC показывает черный экран:

```bash
# Запустите с отладкой
python3 vlc_client.py --server http://server --device test --debug

# Проверьте что файл доступен
curl -I http://server/content/device-id/video.mp4
```

### MPV не может загрузить файл:

```bash
# Увеличьте timeout
# В mpv_client.py добавьте:
'network-timeout': 60  # 60 секунд
```

---

## 📝 Рекомендации

### Для файлов >1GB:

1. **Конвертируйте в web-оптимизированный формат:**
```bash
ffmpeg -i input.mp4 \
  -movflags +faststart \
  -c:v libx264 -preset fast -crf 22 \
  -c:a aac -b:a 128k \
  output.mp4
```

Флаг `-movflags +faststart` перемещает метаданные в начало файла = мгновенный старт!

2. **Используйте h264/h265 кодеки** (hardware acceleration)

3. **Разрешение 1080p или ниже** (4K требует много ресурсов)

### Для множества клиентов (>10):

Используйте SSD вместо HDD для хранения видео

### Для Raspberry Pi:

- Файлы до 1GB работают отлично
- >1GB может требовать больше RAM
- Используйте h264 (аппаратное декодирование)

---

## ✅ Checklist оптимизации:

- [ ] Запустили optimize-for-large-videos.sh
- [ ] Nginx перезагружен без ошибок
- [ ] Обновлены VLC/MPV клиенты (git pull + переустановка)
- [ ] Протестированы большие файлы (>500MB)
- [ ] Логи Nginx не показывают ошибок

---

## 📊 Результат

После оптимизации:
- ✅ Файлы до 5GB загружаются за 5-10 секунд
- ✅ Streaming начинается мгновенно
- ✅ Нет timeout ошибок
- ✅ Плавное воспроизведение

**Важно:** Перезапустите клиенты после обновления кода!

