# Video Control System

Система управления видео-контентом для множественных устройств (ТВ, проекторы и т.д.) с поддержкой видео, изображений, PDF и PowerPoint презентаций.

## 📦 Быстрый старт

**GitHub репозиторий:** https://github.com/ya-k0v/VideoControl

```bash
# Клонировать репозиторий
git clone https://github.com/ya-k0v/VideoControl.git
cd VideoControl

# Установить зависимости Node.js
npm install

# Установить системные зависимости (Ubuntu/Debian)
sudo apt-get install libreoffice graphicsmagick

# Запустить сервер
npm start
```

Сервер запустится на http://localhost:3000

- **Админ панель:** http://localhost:3000/admin.html
- **Плеер:** http://localhost:3000/player.html?device_id=YOUR_DEVICE_ID

## Возможности

- 🎬 **Воспроизведение видео** - поддержка MP4, WebM, OGG, MKV, MOV, AVI
- 🖼️ **Отображение изображений** - PNG, JPG, JPEG, GIF, WebP
- 📄 **PDF презентации** - конвертация в изображения с постраничной навигацией
- 📊 **PowerPoint презентации** - конвертация PPTX в изображения с постраничной навигацией
- 📱 **Адаптивный интерфейс** - работает на PC, планшетах и телефонах
- 🔄 **Real-time управление** - управление устройствами через WebSocket
- ⏸️ **Управление воспроизведением** - Play, Pause, Restart, Stop
- 🌐 **Автономность плеера** - плеер работает независимо, используя кэш заглушки
- 🚀 **Оптимизация производительности** - Nginx раздача контента, оптимизированная загрузка
- 🌍 **Поддержка русских символов** - корректное отображение русских букв в именах файлов
- 🎯 **Нативные клиенты** - VLC (Windows/Linux/macOS) и MPV (Raspberry Pi) клиенты

## Структура проекта

### Файлы в репозитории (24 файла)

```
VideoControl/
├── .gitignore              # Правила игнорирования (node_modules, кэш и т.д.)
├── README.md               # Документация проекта
├── package.json            # Зависимости и скрипты npm
├── package-lock.json       # Точные версии зависимостей
├── server.js               # Основной сервер (Express + Socket.IO)
├── devices.json            # Конфигурация устройств
├── file-names-map.json     # Маппинг имен файлов
├── videocontrol.service    # Systemd service для production
│
├── nginx/                  # Конфигурация Nginx для production
│   ├── videocontrol.conf   # Конфиг Nginx (оптимизированный)
│   ├── install-nginx.sh    # Скрипт установки Nginx
│   ├── test-nginx.sh       # Скрипт тестирования
│   └── README.md           # Документация по Nginx
│
├── clients/                # 🆕 Нативные клиенты для разных платформ
│   ├── README.md           # Общая документация по клиентам
│   ├── vlc/                # VLC клиент (Windows/Linux/macOS)
│   │   ├── vlc_client.py   # Python клиент для VLC
│   │   ├── requirements.txt
│   │   ├── videocontrol-vlc@.service
│   │   └── README.md
│   └── mpv/                # MPV клиент (Raspberry Pi)
│       ├── mpv_client.py   # Python клиент для MPV
│       ├── requirements.txt
│       ├── videocontrol-mpv@.service
│       └── README.md
│
└── public/                 # Публичные файлы (раздаются сервером)
    ├── admin.html          # Панель администратора
    ├── player.html         # Страница плеера
    ├── speaker.html        # Панель спикера
    ├── favicon.ico         # Иконка сайта
    ├── sw.js               # Service Worker для PWA
    │
    ├── css/
    │   └── app.css         # Стили приложения
    │
    ├── js/
    │   ├── admin.js        # Логика админ-панели
    │   ├── player.js       # Логика плеера
    │   ├── speaker.js      # Логика панели спикера
    │   └── theme.js        # Управление темой (светлая/темная)
    │
    └── content/            # Медиа-контент устройств (примеры)
        ├── pc001/          # Контент для устройства pc001
        │   ├── default.mp4 # Заглушка по умолчанию
        │   └── main.mp4    # Основное видео
        └── rpi0001/        # Контент для устройства rpi0001
            ├── default.mp4                          # Заглушка
            ├── Test720.mp4                          # Тестовое видео
            ├── Official music video.mp4             # Видео файл
            └── 20200125110231_Priroda_10-344.jpg    # Изображение
```

### Автоматически создаваемые папки (не в репозитории)

Эти папки создаются автоматически при работе и игнорируются в `.gitignore`:

```
├── node_modules/     # Зависимости (устанавливаются через npm install)
├── .converted/       # Кэш конвертированных PDF в изображения
└── .pptx_cache/      # Кэш конвертированных PPTX в изображения
```

## Требования

- **Node.js** версии 14+ (рекомендуется 18+)
- **LibreOffice** или **soffice** - для конвертации PPTX в изображения
- **ImageMagick** или **GraphicsMagick** - для конвертации PDF/PPTX (устанавливается с pdf2pic)

## Установка

### Из GitHub

1. Клонируйте репозиторий:
```bash
git clone https://github.com/ya-k0v/VideoControl.git
cd VideoControl
```

2. Установите зависимости:
```bash
npm install
```

3. Установите системные зависимости для конвертации PPTX:
```bash
# Ubuntu/Debian
sudo apt-get install libreoffice graphicsmagick

# CentOS/RHEL
sudo yum install libreoffice GraphicsMagick

# macOS
brew install --cask libreoffice
brew install graphicsmagick
```

4. (Опционально) Настройте конфигурацию:
```bash
# Добавьте свои устройства в devices.json
nano devices.json

# Или оставьте примеры устройств pc001 и rpi0001
```

**Примечание:** 
- Папки `node_modules/`, `.converted/`, `.pptx_cache/` создаются автоматически
- Папка `public/content/` уже содержит примеры медиа-файлов для тестирования

## Запуск

### Режим разработки

```bash
npm start
```

Сервер запустится на порту 3000 (или на порту, указанном в переменной окружения `PORT`).

### Режим production (systemd)

1. Отредактируйте `videocontrol.service` - укажите правильные пути:
```bash
# Замените YOUR_USERNAME на ваше имя пользователя
# Замените /path/to/VideoControl на реальный путь к проекту
nano videocontrol.service
```

2. Установите и запустите сервис:
```bash
sudo cp videocontrol.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable videocontrol
sudo systemctl start videocontrol

# Проверить статус
sudo systemctl status videocontrol

# Смотреть логи
sudo journalctl -u videocontrol -f
```

### Режим production с Nginx (рекомендуется) 🚀

**Nginx** значительно ускоряет раздачу видео контента (в 5-10 раз быстрее чем Node.js).

**Преимущества:**
- ⚡ Ускорение раздачи видео в 5-10 раз
- 📊 Поддержка 100+ одновременных соединений
- 🔄 Эффективное использование CPU и памяти
- 🎯 Оптимизированные HTTP заголовки для видео

**Установка и настройка:**

```bash
# 1. Установить и настроить Nginx (один скрипт)
cd /vid/videocontrol/nginx
sudo bash install-nginx.sh

# 2. Проверить работу
bash test-nginx.sh
```

После установки Nginx:
- **Nginx** слушает на порту **80** (публичный доступ)
- **Node.js** работает на порту **3000** (внутренний)
- Доступ к приложению: `http://your-server-ip/`

**Что делает Nginx:**
- Раздает `/content/*` напрямую (видео, изображения) - **БЫСТРО**
- Проксирует API и WebSocket на Node.js
- Кэширует статические файлы
- Поддерживает Range requests для видео seek

**Подробности:** См. [nginx/README.md](nginx/README.md)

### Нативные клиенты (VLC, MPV) 🎬

Помимо браузерного плеера, доступны нативные клиенты для разных платформ:

#### **VLC Client** (Windows/Linux/macOS):

```bash
cd /vid/videocontrol/clients/vlc
pip3 install -r requirements.txt
python3 vlc_client.py --server http://YOUR_SERVER --device vlc-001
```

**Подходит для:**
- Офисные ПК (Windows/Linux/macOS)
- Конференц-залы
- Презентационные экраны

#### **MPV Client** (Raspberry Pi):

```bash
cd /vid/videocontrol/clients/mpv
pip3 install -r requirements.txt
python3 mpv_client.py --server http://YOUR_SERVER --device rpi-001
```

**Подходит для:**
- Raspberry Pi (оптимизировано!)
- Слабые ПК
- Embedded системы

**⚠️ Важно:** Используйте порт 80 (через Nginx) для production. Порт 3000 только для разработки БЕЗ Nginx.

**Особенности клиентов:**
- ✅ Автоматическая заглушка (loop default.mp4)
- ✅ Real-time управление через WebSocket
- ✅ Автономность при потере связи
- ✅ Hardware acceleration (MPV на RPi)
- ✅ Systemd integration для автозапуска

**Подробности:** См. [clients/README.md](clients/README.md)

## Использование

### Доступ к интерфейсам

- **Плеер**: `http://your-server:3000/player.html?device_id=DEVICE_ID`
- **Панель администратора**: `http://your-server:3000/admin.html`
- **Панель спикера**: `http://your-server:3000/speaker.html`

### Регистрация устройства

1. Откройте страницу плеера с параметром `device_id`:
   ```
   http://your-server:3000/player.html?device_id=TV-01
   ```

2. Плеер автоматически зарегистрируется на сервере

3. Устройство появится в панели администратора и спикера

### Загрузка контента

1. Откройте панель администратора
2. Выберите устройство
3. Загрузите файлы (drag & drop или кнопка выбора файлов)
4. Поддерживаемые форматы:
   - **Видео**: MP4, WebM, OGG, MKV, MOV, AVI
   - **Изображения**: PNG, JPG, JPEG, GIF, WebP
   - **PDF**: автоматически конвертируется в изображения
   - **PPTX**: автоматически конвертируется в изображения

### Управление воспроизведением (Панель спикера)

- **Play** - возобновить воспроизведение (продолжает с места паузы)
- **Pause** - поставить на паузу
- **Restart** - перезапустить с начала
- **Stop** - остановить и вернуться на заглушку
- **Next/Previous** - навигация по слайдам (для PDF/PPTX)

### Переименование устройств

В панели администратора:
1. Кликните на имя устройства
2. Введите новое имя
3. Нажмите галочку для сохранения

## API Endpoints

### Устройства

- `GET /api/devices` - получить список устройств
- `GET /api/devices/:id` - получить информацию об устройстве
- `POST /api/devices/:id/register` - зарегистрировать устройство
- `DELETE /api/devices/:id` - удалить устройство
- `PATCH /api/devices/:id/rename` - переименовать устройство

### Файлы

- `GET /api/devices/:id/files` - получить список файлов устройства
- `POST /api/devices/:id/upload` - загрузить файл
- `DELETE /api/devices/:id/files/:name` - удалить файл
- `GET /api/devices/:id/converted/:file/:type/:num` - получить конвертированную страницу/слайд

### Управление

- `GET /content/:device_id/:file` - получить файл для воспроизведения

## WebSocket Events

### Клиент → Сервер

- `player/register` - регистрация плеера
- `player/ping` - heartbeat для проверки активности
- `control/play` - воспроизвести/возобновить
- `control/pause` - поставить на паузу
- `control/restart` - перезапустить
- `control/stop` - остановить
- `control/pdfNext`, `control/pdfPrev` - навигация по PDF
- `control/pptxNext`, `control/pptxPrev` - навигация по PPTX

### Сервер → Клиент

- `player/state` - текущее состояние устройства
- `player/play` - команда воспроизведения
- `player/pause` - команда паузы
- `player/restart` - команда перезапуска
- `player/stop` - команда остановки
- `player/pdfPage` - установить страницу PDF
- `player/pptxPage` - установить слайд PPTX
- `player/pong` - ответ на ping

## Особенности

### Автономность плеера

Плеер полностью автономен:
- Кэширует заглушку через Service Worker
- Работает даже при недоступности сервера
- Автоматически подключается при восстановлении связи

### Оптимизация видео

- Параллельная предзагрузка частей видео (первые 4MB)
- Поддержка Range запросов для быстрого старта
- Кэширование видео файлов
- Оптимизированная загрузка метаданных

### Поддержка русских символов

Система автоматически обрабатывает русские символы в именах файлов:
- Безопасные имена для файловой системы
- Маппинг на оригинальные имена для отображения
- Корректная обработка при загрузке и отображении

### Конвертация файлов

- **PDF**: конвертируется в изображения постранично
- **PPTX**: конвертируется в изображения построчно через LibreOffice
- Кэш конвертированных файлов сохраняется в `.converted/`
- Автоматическая конвертация при первом открытии

## Конфигурация

### Файлы конфигурации

#### `devices.json`
Настройка устройств с их отображаемыми именами:
```json
{
  "pc001": "PC Display 1",
  "rpi0001": "Raspberry Pi Display 1"
}
```
- Ключ - ID устройства (используется в URL плеера)
- Значение - отображаемое имя в админ-панели

#### `file-names-map.json`
Маппинг имен файлов с русскими символами на безопасные имена:
```json
{
  "pc001": {},
  "rpi0001": {}
}
```
Заполняется автоматически при загрузке файлов с русскими символами.

#### `videocontrol.service`
Systemd unit файл для production. **Перед использованием замените:**
- `YOUR_USERNAME` → ваш пользователь
- `/path/to/VideoControl` → реальный путь к проекту

### Переменные окружения

- `PORT` - порт сервера (по умолчанию 3000)
- `NODE_ENV` - окружение (`development` или `production`)

### Настройка сервера

В файле `server.js` можно изменить:
- **Максимальный размер файла**: `1 GB` (строка с `multer`)
- **Порт сервера**: по умолчанию `3000`
- **Пути к контенту**: `public/content/`

## Отладка

Логи сервера выводятся в консоль. Для просмотра логов systemd сервиса:

```bash
sudo journalctl -u videocontrol -f
```

## Безопасность

- Валидация device_id (только безопасные символы)
- Проверка путей файлов (защита от directory traversal)
- Ограничение размера загружаемых файлов
- Санитизация имен файлов

## Технологии

### Backend & Infrastructure:
- **Backend**: Node.js, Express.js, Socket.IO
- **Reverse Proxy**: Nginx (production)
- **File Upload**: Multer
- **PDF Processing**: pdf2pic, pdf-lib
- **PPTX Conversion**: LibreOffice (soffice)

### Frontend & Clients:
- **Web Frontend**: Vanilla JavaScript, HTML5, CSS3
- **PWA**: Service Worker API
- **VLC Client**: Python 3, python-vlc, python-socketio
- **MPV Client**: Python 3, python-mpv, python-socketio

## Лицензия

Проект разработан для внутреннего использования.

## Поддержка

При возникновении проблем проверьте:
1. Установлен ли LibreOffice и доступен ли в PATH
2. Достаточно ли прав на запись в директории `public/content/`
3. Открыт ли порт в файрволе
4. Установлены ли все зависимости (`npm install`)

## Версия

**Текущая версия:** v1.0.2

**Релизы:** https://github.com/ya-k0v/VideoControl/releases

### История версий

**v1.0.2** (2025-11-02) - MAJOR UPDATE 🚀
- 🎬 **VLC и MPV клиенты** - нативные клиенты для Windows/Linux/macOS и Raspberry Pi
- 📱 **PWA поддержка** - установка плеера, спикера и админки как приложения на мобильных
- 🖼️ **Полная поддержка контента** - VLC/MPV теперь воспроизводят видео, изображения, PDF и PPTX
- 📄 **Навигация по презентациям** - кнопки Next/Prev для PDF/PPTX на всех платформах
- 🎯 **Типизация устройств** - иконки, бейджи возможностей, визуализация в админке
- 💾 **Service Worker v2** - ограничение кэша, stale-while-revalidate, автообновления
- ⚡ **Оптимизация больших файлов** - поддержка видео до 5GB+ с плавным streaming
- 🔧 **Скрипты установки** - автоматизированная установка сервера и всех клиентов
- 📊 **Responsive design** - 2 режима layout (вертикальный/горизонтальный)
- 🐛 **Множественные исправления** - порты, регистрация, видимость списков файлов

**Производительность:**
- Nginx раздача: 500-1000 MB/s (10x быстрее)
- Загрузка видео: 0.1-0.3 сек (10x быстрее)
- Большие файлы (1-5GB): 2-10 сек старт (было: timeout)

**Новые клиенты:**
- VLC Client (Python) - Windows, Linux, macOS
- MPV Client (Python) - Raspberry Pi оптимизация
- PWA - Android TV, iOS, планшеты

**v1.0.1** (2025-11-02)
- 🚀 Добавлена поддержка Nginx для production (ускорение раздачи контента в 5-10 раз)
- 📦 Автоматические скрипты установки и тестирования Nginx
- 📊 Оптимизированные HTTP заголовки для видео (Cache-Control, Range requests)
- 🔄 WebSocket проксирование через Nginx
- 📝 Расширенная документация по настройке production окружения

**v1.0.0** (2025-11-02)
- Первый публичный релиз
- Поддержка видео, изображений, PDF и PPTX
- WebSocket управление в реальном времени
- Адаптивные интерфейсы для плеера, админа и спикера
- PWA с offline поддержкой

## Разработка

### Клонирование для разработки

```bash
git clone https://github.com/ya-k0v/VideoControl.git
cd VideoControl
npm install
npm start
```

### Участие в разработке

Для добавления новых функций:
1. Создайте форк репозитория
2. Создайте ветку для новой функции
3. Внесите изменения
4. Создайте Pull Request

## Авторы

**Разработчик:** ya-k0v  
**GitHub:** https://github.com/ya-k0v/VideoControl


