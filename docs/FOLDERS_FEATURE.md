# Функциональность папок с изображениями

## Описание

Система теперь поддерживает загрузку ZIP архивов с изображениями, которые автоматически распаковываются в папки и отображаются как слайд-шоу с навигацией, аналогичной презентациям PPTX/PDF.

## Использование

### 1. Загрузка папки с изображениями

Существует **два способа** загрузки папок с изображениями:

#### Способ 1: Прямой выбор папки (рекомендуется)

1. В админ-панели выберите устройство
2. Нажмите кнопку **"📁 Папка"**
3. В диалоговом окне выберите папку с изображениями
4. Только изображения (PNG, JPG, JPEG, GIF, WEBP) будут добавлены в очередь загрузки
5. Нажмите **"Загрузить"**

**Преимущества**:
- Не нужно создавать ZIP архив
- Быстрее и удобнее
- Поддерживается перетаскивание папок (drag & drop)

#### Способ 2: ZIP архив

1. В админ-панели выберите устройство
2. Нажмите **"📄 Файлы"** или перетащите ZIP файл в зону загрузки
3. Выберите ZIP архив, содержащий изображения (PNG, JPG, JPEG, GIF, WEBP)
4. Архив автоматически распакуется в папку с тем же именем
5. ZIP файл после распаковки будет удален

**Примечание**: Папка с изображениями появится в списке файлов с меткой `FOLDER`.

### 2. Воспроизведение папки

1. В панели спикера (Speaker) выберите устройство
2. Найдите папку в списке файлов (метка `FOLDER`)
3. Нажмите "Превью" для предпросмотра или "▶ Воспроизвести" для показа на устройстве
4. Используйте кнопки навигации:
   - **◀ Назад** - предыдущее изображение
   - **Вперёд ▶** - следующее изображение
   - **✕ Закрыть** - остановить показ

### 3. Навигация

Навигация работает так же, как для PDF/PPTX презентаций:
- Кнопки "Назад" и "Вперёд" переключают изображения
- Изображения переключаются мгновенно благодаря предзагрузке
- Первое изображение показывается с плавным переходом из черного экрана

## Технические детали

### Поддерживаемые форматы

- **Архивы**: ZIP (автоматическая распаковка)
- **Изображения**: PNG, JPG, JPEG, GIF, WEBP
- **Прямой выбор папок**: Поддерживается в современных браузерах (Chrome, Edge, Safari)

### Обработка архивов

1. ZIP распаковывается в папку с именем архива (без расширения .zip)
2. Если изображения находятся в подпапках, они автоматически перемещаются в корень папки
3. Изображения сортируются по имени (с учетом чисел)
4. Пустые подпапки удаляются

### API Endpoints

#### Получить список изображений в папке
```
GET /api/devices/:id/folder/:folderName/images
```

**Ответ:**
```json
{
  "images": ["image_0001.jpg", "image_0002.jpg", ...],
  "count": 10
}
```

#### Получить количество изображений
```
GET /api/devices/:id/folder/:folderName/count
```

**Ответ:**
```json
{
  "count": 10
}
```

#### Получить изображение по индексу
```
GET /api/devices/:id/folder/:folderName/image/:index
```

**Параметры:**
- `index` - номер изображения (1-based)

**Ответ:** Файл изображения

### Socket.IO события

#### Воспроизведение папки
```javascript
socket.emit('control/play', {
  device_id: 'device1',
  file: 'MyFolder' // Имя папки без .zip
});
```

#### Навигация
```javascript
// Предыдущее изображение
socket.emit('control/pdfPrev', { device_id: 'device1' });

// Следующее изображение
socket.emit('control/pdfNext', { device_id: 'device1' });

// Остановить показ
socket.emit('control/stop', { device_id: 'device1' });
```

#### События плеера
```javascript
// Плеер получает команду показать изображение
socket.on('player/folderPage', (imageNum) => {
  // imageNum - номер изображения (1-based)
});
```

## Особенности реализации

### Предзагрузка
Все изображения из папки предзагружаются в кэш при первом открытии, что обеспечивает мгновенное переключение между изображениями.

### Двойная буферизация
Используется два буфера изображений для плавного переключения без мерцания.

### Производительность
- Изображения загружаются параллельно
- Используется кэширование для быстрого доступа
- Автоматическая очистка кэша при смене контента

## Требования к системе

- **Linux**: Требуется утилита `unzip` или `7z` для распаковки ZIP архивов
- **Node.js**: Версия 14.0 или выше
- **Браузер**: 
  - Для прямого выбора папок: Chrome 21+, Edge 13+, Safari 11.1+, Opera 15+
  - Firefox пока не поддерживает выбор папок (используйте ZIP архивы)
  - Поддержка ES6 и Image API

## Установка утилит

### Ubuntu/Debian
```bash
sudo apt-get install unzip
```

### CentOS/RHEL
```bash
sudo yum install unzip
```

### Alpine Linux (Docker)
```bash
apk add unzip
```

## Примеры использования

### Прямой выбор папки (рекомендуется)

1. Создайте папку с изображениями на вашем компьютере:
```
MyPhotos/
├── photo1.jpg
├── photo2.jpg
├── photo3.jpg
└── photo4.jpg
```

2. В админ-панели нажмите **"📁 Папка"**
3. Выберите папку `MyPhotos`
4. Нажмите **"Загрузить"**

### Создание ZIP архива (альтернативный способ)
```bash
# Linux/macOS
zip -r MyFolder.zip ./images/

# Windows (PowerShell)
Compress-Archive -Path .\images\* -DestinationPath MyFolder.zip
```

### Структура архива
```
MyFolder.zip
├── image_001.jpg
├── image_002.jpg
├── image_003.jpg
└── ...
```

или с подпапками (будут автоматически перемещены):
```
MyFolder.zip
├── subfolder1/
│   ├── photo1.jpg
│   └── photo2.jpg
└── subfolder2/
    ├── photo3.jpg
    └── photo4.jpg
```

## Ограничения

- Максимальный размер ZIP архива: 2GB (настраивается в `constants.js`)
- Поддерживаются только изображения (PNG, JPG, JPEG, GIF, WEBP)
- Архив должен содержать хотя бы одно изображение

## Устранение неполадок

### Кнопка "📁 Папка" не работает
- Убедитесь, что вы используете современный браузер (Chrome, Edge, Safari)
- Firefox не поддерживает выбор папок - используйте ZIP архивы
- Попробуйте перезагрузить страницу

### Архив не распаковывается
- Проверьте, установлена ли утилита `unzip` или `7z`
- Убедитесь, что архив содержит изображения
- Проверьте права доступа к папке устройства

### Изображения не отображаются
- Убедитесь, что формат изображений поддерживается
- Проверьте права доступа к файлам (должны быть 644)
- Проверьте консоль браузера на наличие ошибок

### Навигация не работает
- Убедитесь, что тип контента определяется как `folder`
- Проверьте, что socket.io подключение активно
- Проверьте логи сервера на наличие ошибок

### При загрузке папки не все изображения добавились
- Проверьте формат изображений (поддерживаются только PNG, JPG, JPEG, GIF, WEBP)
- Убедитесь, что размер папки не превышает лимит (2GB по умолчанию)
- Проверьте консоль браузера на наличие ошибок

