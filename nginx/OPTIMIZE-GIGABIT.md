# Оптимизация Nginx для гигабитных сетей

## 📊 Текущие настройки (для 1 Гбит = 125 MB/s)

### Размер блоков отправки:
```nginx
sendfile_max_chunk 2m;        # Nginx отправляет блоками по 2MB
```

**Как работает:**
- Файл разбивается на блоки по 2MB
- Каждый блок отправляется отдельно
- Оптимально для balance между латентностью и throughput

### Буферы:
```nginx
output_buffers 8 256k;        # 8 буферов по 256KB = 2MB total
postpone_output 1460;         # Минимальный размер TCP пакета
```

**Расчет для 1 Гбит:**
- 1 Гбит = 125 MB/s
- Блок 2MB отправляется за ~16ms
- 8 буферов позволяют buffering для плавности

### Асинхронный I/O:
```nginx
aio threads=default;          # Thread pool для чтения с диска
aio_write on;                 # Асинхронная запись в сокет
directio 16m;                 # Прямой I/O для файлов >16MB
```

### TCP оптимизация:
```nginx
tcp_nopush on;               # Накапливаем данные (эффективнее для больших файлов)
tcp_nodelay off;             # Выключен (конфликт с tcp_nopush)
```

## 🚀 Применение оптимизаций

**1. Обновите конфиг:**
```bash
cd /vid/videocontrol
git pull
sudo cp nginx/videocontrol.conf /etc/nginx/sites-available/videocontrol.conf
```

**2. Проверьте конфигурацию:**
```bash
sudo nginx -t
```

**3. Примените:**
```bash
sudo systemctl reload nginx
```

## 📈 Ожидаемая производительность

**До оптимизации:**
- Скорость: ~80-100 MB/s
- Блоки: неограничены (могут быть огромными)
- Латентность: высокая

**После оптимизации:**
- Скорость: ~100-125 MB/s (максимум 1 Гбит)
- Блоки: стабильные 2MB
- Латентность: низкая (~16ms на блок)

## 🎯 Для еще более быстрых сетей

### 10 Гбит (1250 MB/s):
```nginx
sendfile_max_chunk 8m;
output_buffers 16 512k;
directio 64m;
```

### 100 Мбит (12.5 MB/s):
```nginx
sendfile_max_chunk 512k;
output_buffers 4 128k;
directio 4m;
```

## 🔍 Мониторинг

**Проверка скорости:**
```bash
# На клиенте
wget http://SERVER/content/DEVICE/video.mp4 -O /dev/null

# Должно показать ~100-125 MB/s
```

**Логи Nginx:**
```bash
tail -f /var/log/nginx/videocontrol_access.log
```

## ⚙️ Дополнительные настройки /etc/nginx/nginx.conf

Добавьте в `http {}` блок:

```nginx
# Worker оптимизация
worker_processes auto;
worker_rlimit_nofile 65535;

events {
    worker_connections 4096;
    use epoll;
    multi_accept on;
}

http {
    # Основные оптимизации
    sendfile on;
    tcp_nopush on;
    
    # Keepalive для переиспользования соединений
    keepalive_timeout 65;
    keepalive_requests 1000;
    
    # Буферы клиента
    client_body_buffer_size 256k;
    client_max_body_size 1024m;
    
    # Output буферы (для всех location)
    postpone_output 1460;
    
    # Кэш открытых файлов (критично для производительности!)
    open_file_cache max=10000 inactive=60s;
    open_file_cache_valid 120s;
    open_file_cache_min_uses 2;
    open_file_cache_errors on;
}
```

## 🎬 Результат

**Производительность на 1 Гбит:**
- ✅ Видео 100MB стартует за <1 секунду
- ✅ Видео 1GB стартует за <2 секунды  
- ✅ Видео 5GB стартует за <3 секунды
- ✅ Плавный streaming без буферизации
- ✅ Поддержка 50+ одновременных клиентов

